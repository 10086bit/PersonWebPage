<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zqt.dao.adminDao">
 <select id="selectUser" parameterType="admin" resultType="admin">
     select * form admin where name=#{name } and passwd=#{passwd}
 </select>


</mapper>